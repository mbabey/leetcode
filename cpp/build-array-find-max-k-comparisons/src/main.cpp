#include <iostream>

using namespace std;

class Solution {
public:
    int numOfArrays(int n, int m, int k) {
        long ans{0};
        
        if (m < k || k == 0) // No satisfactory array can be constructed
        {
            return 0;
        }
        
        vector<vector<vector<long>>> dp;
        for (int)
        
        
        return static_cast<int>(ans % 1000000007);
    }
};

int main()
{
    
    
    return EXIT_SUCCESS;
}
